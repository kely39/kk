spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kely39/config-repo.git
          search-paths: kk # git仓库地址下的相对地址，可以配置多个，用,分割。
          #label: master
          #username: xxx
          #password: xxx